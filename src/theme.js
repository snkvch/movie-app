import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff4',
      contrastText: '#fff',
    },
    secondary: {
      main: '#495867',
    },
  },
});

export default theme;
