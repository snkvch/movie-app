import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff4',
      contrastText: '#fff',
    },
    secondary: {
      main: '#4579d',
    },
  },
});

export default theme;
